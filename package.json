{
  "name": "using-webpack",
  "version": "1.0.0",
  "description": "* Bundle files from many different sources into a few files (bundle assets) / manage dependencies.\r * loaders\r * create-react-app and other cli's use webpack\r * minified files (bundle.js, main.chunk.js) \"eval\" \"__webpack__require__\" etc.\r * development and production modes\r ## Part 2\r * Sample app -- a calculator with basic validations\r * ./assets, src/app.js, index.html \r * getting a more complicated structure: break app (simple as it is) into utils\r * Why webpack?  Apparently import statements don't work in JS files linked from html!  We would have to manually link everything in the right order (or use dynamic imports, which is annoying). Webpack would make sure that client-side JS has all the dependencies it needs to run.",
  "main": "app.js",
  "scripts": {
    "start": "webpack-dev-server --config './webpack.config.js' --hotOnly --open",
    "build": "webpack --config './webpack.config.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/firezdog/webpack.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/firezdog/webpack/issues"
  },
  "homepage": "https://github.com/firezdog/webpack#readme",
  "devDependencies": {
    "bootstrap": "^4.3.1",
    "clean-webpack-plugin": "^2.0.1",
    "css-loader": "^2.1.1",
    "file-loader": "^3.0.1",
    "html-loader": "^0.5.5",
    "html-webpack-plugin": "^3.2.0",
    "jquery": "^3.3.1",
    "node-sass": "^4.11.0",
    "popper.js": "^1.15.0",
    "sass-loader": "^7.1.0",
    "style-loader": "^0.23.1",
    "webpack": "^4.29.6",
    "webpack-cli": "^3.3.0",
    "webpack-dev-server": "^3.2.1",
    "webpack-merge": "^4.2.1"
  }
}
